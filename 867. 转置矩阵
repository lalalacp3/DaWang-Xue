class Solution:
    def transpose(self, A: List[List[int]]) -> List[List[int]]:
        m=len(A)
        n=len(A[0])
        B = [[] for x in range(n)] 
        for i in range(m):
            for j in range(n):
                a=A[i][j]
                B[j].append(a)
        return B
